'use strict';

/**
 * features-jumbotron service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::features-jumbotron.features-jumbotron');
