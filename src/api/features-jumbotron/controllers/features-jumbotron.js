'use strict';

/**
 * features-jumbotron controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::features-jumbotron.features-jumbotron');
