'use strict';

/**
 * features-jumbotron router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::features-jumbotron.features-jumbotron');
