'use strict';

/**
 * features-manajeme service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::features-manajeme.features-manajeme');
