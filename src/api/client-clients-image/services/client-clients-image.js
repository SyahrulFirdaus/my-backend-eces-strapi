'use strict';

/**
 * client-clients-image service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::client-clients-image.client-clients-image');
