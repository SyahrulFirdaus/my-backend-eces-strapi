'use strict';

/**
 * features-marketing router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::features-marketing.features-marketing');
