'use strict';

/**
 * features-marketing service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::features-marketing.features-marketing');
